#!/usr/bin/env bash

echo 'Run `vagrant ssh`, log in, run `cd '"${SCRIPT_DIR}"'`, and then run `./run-server.sh` to start the server.'

echo 'Alternatively, you can run "run-server.sh" on your host machine.'

echo 'It will also check if the server is already running.'

